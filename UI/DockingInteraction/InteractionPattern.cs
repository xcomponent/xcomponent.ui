namespace XComponent.Common.UI.DockingInteraction
{
    public enum InteractionPattern
    {
        MasterSlave,
        Enslave,
        PeerToPeer,
        SelectedParticipant,
        Broadcast,
    }
}